import React from 'react';
import OnboardingClient from './OnboardingClient';

export const metadata = {
  title: 'Welcome to ViralPost — Quick setup',
};

export default function OnboardingPage() {
  return <OnboardingClient />;
}
